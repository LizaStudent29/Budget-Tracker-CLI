# budget-tracker-cli

**budget-tracker-cli** — учебное консольное приложение на TypeScript для учёта личного бюджета.  
Позволяет создавать счета (бюджеты), добавлять доходы и расходы, удалять транзакции и экспортировать их в CSV.

Проект сделан как CLI-приложение: всё управление идёт через интерактивное меню в терминале.

---

## 🔧 Стек технологий

- **Node.js** + **npm**
- **TypeScript**
- **ES-модули** (`"type": "module"`)
- **tsx** — для запуска TypeScript без ручной сборки
- **inquirer** — интерактивные вопросы в консоли
- **uuid** — генерация уникальных идентификаторов
- небольшая утилита `budget-utils` для форматирования валюты
- экспорт в CSV через `fs/promises`

---

## 📦 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
git clone <url-репозитория>
cd budget-tracker-cli
npm install
```

### 2. Запуск приложения

```bash
npm start
```

Приложение запустится в терминале и покажет главное меню.

> Навигация по меню сделана **по вводу номера** — просто вводите цифру пункта и нажимаете Enter.

### 3. (Опционально) Сборка TypeScript

```bash
npm run build
```

Скомпилированные файлы попадут в папку `dist/`.

---

## 🗂 Структура проекта

```text
budget-tracker-cli/
├── src/
│   ├── classes/
│   │   ├── Account.ts
│   │   ├── AccountManager.ts
│   │   └── Transaction.ts
│   ├── interfaces/
│   │   ├── TransactionType.ts
│   │   ├── ITransaction.ts
│   │   ├── IAccount.ts
│   │   ├── ISummary.ts
│   │   ├── IAccountManager.ts
│   │   └── utility-types.ts
│   ├── utils/
│   │   └── escapeCsvValue.ts
│   ├── lib/
│   │   ├── budget-utils.js
│   │   └── budget-utils.d.ts
│   ├── ApplicationController.ts
│   └── index.ts
├── dist/
├── tsconfig.json
├── package.json
└── README.md
```

---

## 🧮 Основные сущности

### **Account (счёт)**  
Хранит список транзакций и вычисляет баланс.

### **Transaction (транзакция)**  
Доход или расход с датой, суммой и описанием.

### **AccountManager**  
Создание / хранение / удаление счетов.

---

## 🕹 Логика CLI

Приложение позволяет:
- просматривать список счетов,
- создавать новые счета,
- добавлять и удалять транзакции,
- экспортировать данные в CSV,
- удобно перемещаться между меню.

Меню работает через **ввод номера** (1, 2, 3…).

---

## 🧪 Экспорт в CSV

Каждая строка CSV имеет формат:

```
id,amount,type,date,description
```





